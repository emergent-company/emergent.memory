node_modules/
dist/
.next/

# MkDocs build output (root-level site/ only, not docs/site/ which is our source)
/site/

# Ignore build outputs in all packages
**/dist-openapi/

# Environment files with secrets (never commit)
.env.local
**/.env.local
.env.*.local
.env.e2e.scenarios
.env.infisical
docker/.env

# Environment files for specific deployments (never commit)
.env.staging
.env.production

# Backup and temporary files
*.bak

# Backup and temporary env files (never commit)
*.env.bak
*.env.backup*
.env.remote
.env.opencode
.env.dev.local

# Note: .env files WITH SAFE DEFAULTS are committed
# All secrets should be in .env.local files only

# Credentials files
credentials.txt
**/credentials.txt

.DS_Store

# Secrets directory (service account JSON keys)
secrets/
!secrets/README.md

# Database backups and schema dumps
backups/

# Playwright / E2E artifacts
playwright-report/
**/playwright-report/
test-results/
**/test-results/
/.cache/ms-playwright/
**/.cache/ms-playwright/

# Coverage outputs
coverage/
**/coverage/
lcov.info

# Vitest / Jest snapshots (optional keep if needed)
*.snap

# Misc build caches
.nx/
.vite/
**/.vite/

# OS / IDE extras
.idea/
*.log
*.tsbuildinfo

# Development PID files
.dev-pids/
apps/pids/

# Misc
*.log

storybook-static
logs

# Duplicate detection reports
/reports/jscpd/
apps/server/server
spec-server-dev-vertex-ai.json
.gemini/

# Built CLI binaries
tools/cli/emergent-cli-*
tools/cli/emergent-darwin-*
tools/cli/emergent-linux-*
tools/cli/emergent
bin/
apps/server/tmp/

# Go build artifacts and temporary files
*.exe
*.exe~
*.dll
*.so
*.dylib
/server
/extraction-bench
/imdb-bench
/seed-imdb
/seed-lovdata
/imdb-benchmark-runner
/lovdata-benchmark-runner

# Temporary Go scripts and patches
fix_*.go
patch*.go
test_*.go
setup_*.go
run_*.go
new_*.go
old_*.go
create_project.go
clean_syntax.go

# Temporary shell scripts
*.sh.tmp
test-*.sh
run_*.sh
get_*.sh

# Benchmark and test runners
apps/server/benchmark_script
apps/server/extraction-bench
apps/server/seed-imdb
apps/server/seed-lovdata
apps/server/main

# Root level Go module files (should be in subdirectories)
/go.mod
/go.sum

# MP3 and large media files
*.mp3
**/all_mp3s/
**/sample_mp3s/

# Binary executables in tools
tools/niezatapialni-scraper/niezatapialni-scraper

# Large data files
*.jsonl
!blueprints/**/seed/**/*.jsonl
*.log
.claude/settings.local.json

# auto-added by commit skill — 2026-03-01
/cmd
tools/cli/test_search
pids/
tools/cli/.env

# auto-added by commit skill — 2026-03-06
vertex-sa-key.json
tui.json
opencode.jsonc.tui-migration.bak
docs/hooks/__pycache__/
apps/server/swiftbridge

# auto-added by commit skill — 2026-03-09
apps/server/VERSION
