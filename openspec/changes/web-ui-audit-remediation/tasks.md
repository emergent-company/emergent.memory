## 1. Build-input hygiene

- [ ] 1.1 Key `vendor/` generation to the pinned dependency version instead of directory-absence: change `Taskfile.yml:27` so the vendored go-daisy tree is regenerated when the version pinned in `go.mod` differs from the one already vendored (not only when the directory is missing). `vendor/` is untracked (`apps/web-ui/gateway/.gitignore:3`) and is a genuine CSS build input — `webui/css/app.css` `@source`s the vendored components and `@import`s the vendored `components/css/custom.css` (`Taskfile.yml:22-27`) — so a stale tree silently compiles library source that disagrees with `go.mod`. For Go compilation the module cache at the pinned version is authoritative, so a stale tree does not break the build; it misleads the CSS build.
- [ ] 1.2 Document and enforce a reproducible policy for the tree, since it is gitignored: CI SHALL run the CSS build (which validates/regenerates the tree for the pinned version) rather than depending on a skipped optional test. Add a check that fails when a present `vendor/` tree disagrees with the `go.mod` pin, wired into the CSS build path. Unit-level companion: `apps/web-ui/gateway/vendor_consistency_test.go`, skipping only when `vendor/` is absent, with a comment recording that CI (no `vendor/` present) is covered by the CSS-build regeneration instead.
- [ ] 1.3 Confirm after 1.1 that a fresh `task css` regenerates the tree at the pinned commit and that `go build -mod=vendor ./...` succeeds when the tree is present.
- [ ] 1.4 Do **not** edit the vendored `custom.css`. It is generated from go-daisy and any local edit is reverted by the next `go mod vendor`. Its brace nesting and dead daisyUI-v4 variables are upstream defects — tracked in §7.9.
- [ ] 1.5 Stop mounting/serving the unused go-daisy prebuilt bundle at `/static/*` (`main.go:206`), or stop serving `staticfs/static/css/app.css` specifically, after confirming no page links it (the only remaining references are in `css_consolidation_test.go`) and no JS asset under `/static/` is still required. Extend `css_consolidation_test.go` to assert the bundle is not reachable.
- [ ] 1.6 Delete the dead `.row-actions` / `tr[data-row]:hover .row-actions` rules (`webui/css/app.css:1082-1088`) after re-confirming zero references across `*.templ`, `*.go`, and `*.js`.
- [ ] 1.7 Verification: `templ generate ./...`, `go build ./...`, `go build -mod=vendor ./...` (with a vendored tree present), `go test ./...`, `task lint`, `task css` from `apps/web-ui/gateway` all succeed; compiled CSS is byte-identical across two builds.

## 2. Single-source the duplicated patterns

### 2.1 Copy-to-clipboard

- [ ] 2.1.1 Add `components.CopyButton(props)` (label + variant + target id / literal value + optional reveal-fetch) and `components.CopyScript()` to the `components` package; keep it domain-free per `components/doc.go`.
- [ ] 2.1.2 Unit test: rendered `CopyButton` emits the expected `data-copy-target`/variant markup and `CopyScript` declares exactly one copy implementation. Test: `components/copy_test.go`.
- [ ] 2.1.3 Migrate the four canonical call sites (`api_tokens.templ:452`, `share_owner.templ:33`, `mcp_shares.templ:76`, `agent_mcp_endpoint.templ:144`; plus the `data-copy-target` attribute at `agent_mcp_endpoint.templ:134`) to the component without changing rendered output.
- [ ] 2.1.4 Replace the duplicate implementation in `share_manage.templ`: delete `shareManageScript`/`copyText` (`:618-628`), replace the raw buttons at `:589`/`:605`, and fold the reveal-fetch behaviour into the shared component.
- [ ] 2.1.5 State in the requirement that the snippet and one-time-secret-reveal components consume the shared copy component, so the three copy surfaces are one contract rather than three.
- [ ] 2.1.6 Unit test: the deleted duplicate no longer exists, and each migrated page's copy control targets the right element id.

### 2.2 Toast dispatch

- [ ] 2.2.1 Single-source toast *dispatch* without regressing PRG feedback. The server-rendered flash path (`flashToast`, `ui.go:157-168`) already emits an inline script that uses the Alpine `#toast-container` queue when the runtime is up and otherwise stashes under `sessionStorage["memory-toast"]` for `app.js`'s `flushStashedToast` on `DOMContentLoaded`. Ordering matters: `appShell` renders `@content` before it loads `app.js` (`ui.templ:133` vs `:168`), so content-positioned flash scripts run before `window.MemoryApp` exists. Do NOT replace the flash path with a direct `MemoryApp.toast` call; instead make the flash path and `MemoryApp.toast` share one implementation that keeps the queue-or-stash fallback.
- [ ] 2.2.2 Delete the third toast implementation in `mcp_servers.templ:565-575` and call the shared entry point; keep the `memory-toast` listener contract in `project_settings.templ:1247` working.
- [ ] 2.2.3 Unit test: one client dispatch implementation remains, with the server-rendered flash renderer kept distinct. Add explicit coverage that a flash toast still surfaces on a **full page load** (not only after an HTMX swap); update `refactor_exact_test.go`'s `flashToasts` golden deliberately if its output changes, and record the normalization.

### 2.3 Table shell

- [ ] 2.3.1 Decide and implement one table story. Only three tables are genuinely unwrapped (`migrations.templ:344,441`, `blueprints.templ:759`); `api_tokens.templ:126` and `agent_mcp_endpoint.templ:237,478` already render inside `components.TableCard` and need no wrapping. Either extend the shared shell to cover the three unwrapped tables, or adopt the go-daisy `table.TableWithProps` primitive and drop the extra `overflow-x-auto`. **This rescinds the earlier deferral** recorded in the main `web-ui-components` spec — the deferral's reason (the primitive always emits its own `overflow-x-auto`, adding a nesting level) must be re-examined explicitly, and the decision plus its reason recorded in the change README.
- [ ] 2.3.2 Apply the chosen story to the three unwrapped tables, preserving headers, row contents, `data-testid`s, and column alignment. If the decision is to adopt `table.TableWithProps`, the inner table becomes `class="table"` and `api_tokens_ui_test.go:60` (which asserts the literal `"<table class=\"table table-sm\">"`) **must** be updated in this task — declare that golden change explicitly; the shell-only option avoids it.
- [ ] 2.3.3 Unit test: no page declares a bordered zero-padding table card outside the shared component, and the one-table-story decision is asserted (either every table sits in the shared shell, or every table renders through the chosen primitive).

### 2.4 Form controls

- [ ] 2.4.1 Route the 13 raw controls in `project_settings.templ` (`:174,177,180,187,207,282,285,288,291,294,322,353,366`) through `settingsField`/`form.FormControl`, preserving ids, `name`s, `hx-*` attributes, and autosave behaviour.
- [ ] 2.4.2 Route the raw controls in `schema.templ` (`:383,390,485,486,496,506,520`), `sidepanel.templ:116`, and `share_page.templ:303,512,574`.
- [ ] 2.4.3 Unit test: no raw `input w-full` / `textarea` / `select` remains outside the form primitives among the 24 scoped sites (excluding intentional bare toggles already covered by the `ToggleInput` requirement, and date/chip inputs named as out of scope).

### 2.5 Remaining repeated primitives

- [ ] 2.5.1 `components.StatGrid` and migrate the `backups.templ:461-465` custom stat grid; unit test `components/stat_grid_test.go`; confirm `ui.StatCard`/`statRow` sites are unchanged.
- [ ] 2.5.2 `components.InlineCode` and migrate `backups.templ:436,442,505,561`; unit test `components/inline_code_test.go`.
- [ ] 2.5.3 Adopt `ui.SoftBox` (or `components.InfoBox`) for the soft panels (strict count: 17 occurrences across 12 files; 33 if the looser `bg-base-200/30|40` match is used); unit test asserts no hand-written translucent bordered box remains outside the component, keeping it distinct from the standard opaque `PanelCard`.
- [ ] 2.5.4 Adopt `components.MetaRow`/`MetaGrid` at the raw `<dt>/<dd>` sites (`backups.templ:421,434,440`; `backups.templ:560` is an uppercase label inside `backupChecksumBlock`, not a term/description pair).
- [ ] 2.5.5 Adopt `ui.Eyebrow` for the raw uppercase labels (`share_manage.templ:294,314`, `migrations.templ:437`, `mcp_shares.templ:350`), declaring the `nav.PageHeading` kicker exempt (or stating that `PageHeading` uses `ui.Eyebrow`) so the kicker is not double-owned.
- [ ] 2.5.6 Migrate the raw `btn btn-*` occurrences to `ui.Button` — 19 occurrences across 8 templates: `schema.templ:433,461,521,546,549,583`, `blueprints.templ:643`, `project_settings.templ:840`, `share_manage.templ:591,607`, `chat_dock.templ:44,45,46,78,79`, `agent_mcp_endpoint.templ:434`, `org_context.templ:403`, `usage.templ:63`. (`chat_dock.templ:65` and `:92` are *not* `btn btn-*` — they are `dock-question-option` and `todo-card-toggle`.) Class order changes on some sites (e.g. `project_settings.templ:840`), which no test pins.
- [ ] 2.5.7 Adopt `ui.EmptyState` **only** at sites that are genuine empty-state blocks: `share_page.templ:494`, `schedules.templ:293`, `project_settings.templ:405,889`, `org_members_ui.templ:92`, `approvals.templ:29`. Explicitly **excluded**: `auth_ui.templ:223` (a dropdown `<li>` placeholder), `chat.templ:524` (`chatRailEmpty` carries the stable ids `#chat-rail-empty`/`#chat-rail-empty-filtered` that `chat.js` toggles, and `ui.EmptyState` has no wrapper-id prop), and the `cardList` empty branch in `ui.templ`, which `refactor_exact_test.go:79` pins byte-exactly. Where adoption changes rendered output (e.g. `share_page.templ:494`'s `data-testid`), declare the normalization in 2.5.9.
- [ ] 2.5.8 Unit test per extraction group asserting the pattern now exists only in the shared component, with the exclusions in 2.5.7 asserted as intentional.
- [ ] 2.5.9 Reconcile rendered output in **both** the `web-ui-components` "adoption preserves rendered output" and "extraction preserves rendered output" requirements: enumerate every normalization this change introduces — button class order, the table-wrap change, the empty-state adoptions, the muted-text/icon-scale class strings from §3, and the §2.6 consolidations — and for each pinned assertion decide whether it is a **contract** assertion (keep) or a **class-only** assertion (narrow or remove, per §2.6.6).
- [ ] 2.5.10 Phase verification: exact-render and DOM-pinning tests pass with goldens updated only where a normalization was declared; `templ generate`, `go build`, `go vet`, `go test`, `task lint`, `task e2e:test` all green.

### 2.6 Surface consolidation

- [ ] 2.6.1 One card component plus a card list. Fold the gateway's own card usage — `components.PanelCard`, `components.TableCard`, and the hand-rolled `card card-border` sites (`approvals.templ:27,44`, `share_manage.templ:224,414`) — into one local component with a size/density variant that delegates to a single library primitive; resolve the `shadow-sm` divergence between `table.templ:11` and `panel.templ:14`; keep `PanelCard`/`TableCard` as call-site shims only until their callers migrate, then delete them. **Scope:** folding `ui.CardRaw`/`ui.Card`/`ui.Section` chrome into one *upstream* component is the §7 dependency and is not done here.
- [ ] 2.6.2 One form-field component (label, hint, error, optional tip) replacing `form.FormControl`, the page-local settings field (`project_settings.templ:19`), `components.ToggleField`, and the raw `<fieldset>`/`<legend>` controls (`api_tokens.templ:277-280`); the toggle requirement still applies to bare toggles.
- [ ] 2.6.3 One page-header component with optional kicker, breadcrumbs, and leading content, plus an actions slot; delete `detailHeader` (`ui.templ:642`) in favour of `pageHeader` (`ui.templ:584`) with optional parts, and move the baked `mb-6`/`mb-4` margins (`ui.templ:595,607`) to callers.
- [ ] 2.6.4 One shared tone type. Introduce the gateway-local tone type and route the ~28 `*Intent` mappers through it; remove the domain-to-tone map from the gateway's shared layer. **Scope:** renaming the library's own prop spellings (`Badge.Variant`, `Alert.Type`, `IconTile.Tone`, `StatCard.IconColor`) is the §7.13 dependency; here the gateway adapts behind one local type.
- [ ] 2.6.5 Remove layout ownership from the gateway's own components: replace the local `Class` escape hatches, the stat-row margin parameter (`ui.templ:279`), and `cardListProps.Margin` (`ui.templ:780-790`) with density variants, and introduce one page-rhythm utility for inter-component spacing. **Scope:** removing `Margin`/`CardClass`/`BodyClass` from the library's `ui.Section` is the §7.10 dependency.
- [ ] 2.6.6 Apply the test policy: keep contract assertions (ids, `data-testid`, `aria-*`, `hx-*`, client markers) and **narrow or remove** class-only/class-order assertions (`refactor_exact_test.go:79,93-94`, `visual_delta_pinning_test.go`, `adoption_contract_test.go`, `api_tokens_ui_test.go:60`). Where a class assertion is genuinely load-bearing, move it to `testdata/*.golden` behind an explicit `-update` flag.
- [ ] 2.6.7 Verification: `templ generate`, `go build`, `go vet`, `go test`, `task lint`, gallery render check, and `task e2e:test`; confirm the narrowed test suite still fails if a contract attribute is removed (mutation-check at least one component).

## 3. CSS consolidation and theming

- [ ] 3.1 Reduce the JS-injected duplicate rules in `webui/static/js/chat-components.js:172,473` to a documented, tested subset. It cannot simply be deleted: the active `error-contrast-remediation` change adds a `web-ui-css` requirement that the injected badge rules stay in sync with `app.css`, so this task makes the subset explicit and keeps the sync test green.
- [ ] 3.2 Introduce a muted-text scale (~4 tokens replacing the 12 opacities) and a canonical icon size set (`size-3.5/4/5`), then migrate the call sites; unit/style test asserts no opacity outside the scale is used in the gateway templates.
- [ ] 3.3 Token-ize the hardcoded colours: the four `#0E1017` FOUC guards (`ui.templ:77`, `auth_ui.templ:59`, `share_page.templ:141`, `app.css:137`), `.memory-monogram` (`app.css:265-272`), `.chat-bubble-neutral` (`app.css:298`), `.memory-md pre` (`app.css:970`), and the JS `#fff` mix (`chat-components.js:203`).
- [ ] 3.4 De-triplicate the inline FOUC `<style>` guard into one shared head partial.
- [ ] 3.5 Move the page-local `<style>` blocks that duplicate shared idioms (`chat.templ:270`, `auth_ui.templ:60-129`, `share_page.templ:142-239`) into the shared stylesheet, keeping each page's self-contained-shell intent.
- [ ] 3.6 Replace the hand-rolled badge/button/card/drawer/modal classes the audit listed with daisyUI or the shared components (`.memory-rail-badge`, `.dock-count`, `.todo-item-status`, `.dock-card`, `.memory-queue-send`/`-remove`, `.memory-copy-btn`, `.login-card`, `.share-rail`, `#sidepanel-modal-box`).
- [ ] 3.7 Verification: run `task css` first — the compiled-CSS assertions in `css_consolidation_test.go` **skip** when `webui/static/css/app.css` is absent, so `go test ./...` alone does not exercise them. Then: `task css` twice → byte-identical; `css_consolidation_test.go` green; `task e2e:test` green (Phase 3 changes the compiled CSS every page depends on); manual browser pass over the pages touching changed CSS.

- [ ] 3.8 Migrate literal radius in our templates to theme-driven radius: the 20 `rounded-lg` (`api_tokens.templ:423`, `skills.templ:302`, `share_page.templ:397,398,462`, `mcp_servers.templ:417`, `project_settings.templ:40`, `agent.templ:59,497`, `share_manage.templ:512,564`, `components/nav.templ:19`, `org_context.templ:845`, `mcp_shares.templ:353`, `approvals.templ:69`, `ui.templ:183`, `schedules.templ:320`, `backups.templ:91,95,481`), the 7 `rounded-md` (`share_manage.templ:521`, `proposal.templ:232`, `migrations.templ:294,305,316,326,335`), the 3 bare `rounded` (`backups.templ:436,442,505`), and `ui.templ:1026` `rounded-2xl` → `rounded-box` for containers/tiles, `rounded-field` for controls and inline chips. Leave `rounded-full` where it is a deliberate circle (avatars, status dots, pills).
- [ ] 3.9 Convert the card-like radius literals in `app.css` to theme variables: `#chat-rail .list-row:1127`, `.dock-card:663`, `.todo-card:745`, `.memory-queue-row:575`, `.dock-question-option:722`, `.memory-run-marker-failure:448`, `.memory-md pre:975`, blockquote `:1000`, `.memory-copy-btn:508`, queue-input focus `:595`, todo status `:792` → `var(--radius-box)` / `var(--radius-field)` / `var(--radius-selector)`.
- [ ] 3.10 Make density one central edit: remove the component-local padding overrides on daisyUI roots (`components/panel.templ:14` `p-5`, `components/table.templ:11` `p-0`, `components/secret.templ:109` `p-5`, `blueprints.templ:308` `p-0`, `ui.templ:306-308` `p-4`, `share_manage.templ:225,416`, `approvals.templ:28,45`) and declare them in one documented density block instead. daisyUI has **no padding token**, so the block uses component-internal vars (`--card-p`, `--btn-p`) and layered overrides; document it as non-semver-stable and re-check on daisyUI upgrades. Also document `html{font-size:15px}` (`app.css:140`) as the deliberate global rem lever that scales all theme geometry.
- [ ] 3.11 Give the page background one source: consolidate `app.css:137` and the three inline FOUC guards (`ui.templ:77`, `auth_ui.templ:59`, `share_page.templ:141`) onto one token-derived declaration, and co-locate + annotate the `theme-color` metas (`ui.templ:35`, `auth_ui.templ:25`, `share_page.templ:111`) and `static/manifest.webmanifest:8` as literals that cannot read CSS variables and must be updated together.
- [ ] 3.12 Add the temporary `@theme inline` radius alias mapping Tailwind's radius namespace to the semantic tokens, so the literal radius still present in library markup (§7.15) follows the theme until upstream is converted; declare it once, mark it as a bridge, and remove it when §7.15 lands.
- [ ] 3.13 Verification: with `task css` run first, change `--radius-field` and `--radius-box` in the theme block and confirm buttons, fields, cards, dialogs, and badges all change without a template edit; confirm a single density-block edit changes component padding everywhere; confirm no literal radius remains on controls or containers.

## 4. UX and information architecture

- [ ] 4.1 Settle the settings IA, modifying the `settings-navigation` capability rather than adding a parallel contract: the rail becomes the navigator for the Settings area, listing the sidebar Settings group's members, with the six project-setting sections (General, Assistant, Overrides, Providers, Voice, Devices) nested under the Project entry. Render the rail on every Settings-group destination. Approvals is currently a top-level ungrouped nav item (`ui.go:75-82`) and MCP sharing is a nested page (`/settings/mcp-servers/shares`), so the change must either adopt them into the group (approvals as an entry, sharing as a child view of MCP servers) or explicitly exclude them — the rail contents and the group must end up identical. Unit test asserts the rail entries equal the sidebar group's entries for every settings route, and that the six sections stay reachable.
- [ ] 4.2 Standardize on one page-header component per page class, and define the kicker vocabulary **as a shared source of truth** (a kicker→sidebar-group mapping), since kickers are currently free-form strings and the rule is not testable without one. Unit test asserts every page's kicker resolves to its sidebar group through that mapping.
- [ ] 4.3 Adopt one destructive-confirmation convention (modal `ui.ConfirmDialog`) for every destructive POST. This requires an **exhaustive inventory first** — every route that deletes, revokes, removes, rotates, unapplies, or reduces access — then migration of each site that does not already use `ui.ConfirmDialog`. Native `hx-confirm`: `documents.templ:105,152`, `api_tokens.templ:194,215`, `share_manage.templ:234,247`. `window.confirm`: `org_context.templ:862`, `org_members_ui.templ:477` (member role change, which removes and re-invites). Bespoke `ui.Dialog` confirmations: `mcp_shares.templ:157` (revoke), `:192` (rotate), `mcp_nodes.templ:148` (node removal).
- [ ] 4.3.1 Update the two existing flows that currently act on plain activation, in the same change: `blueprint-gallery` "Remove a pack from the UI" / scenario "Remove a pack" (`openspec/specs/blueprint-gallery/spec.md:49-54`) and `project-settings-ui` "Edit agent definition overrides" (the override-removal path) — route both through `ui.ConfirmDialog`.
- [ ] 4.3.2 Add a guard test over all destructive POST routes asserting each is reachable only through a `ui.ConfirmDialog`, so the inventory cannot silently fall behind as routes are added.
- [ ] 4.4 Add confirmation to the seven unprotected destructive actions: Blueprints unapply (`blueprints.templ:103`), member remove (`org_members_ui.templ:248`), invite revoke (`:311`), agent-override delete (`project_settings.templ:264`), device revoke (`:438`), provider remove (`:916`), org tool delete (`org_context.templ:818`).
- [ ] 4.5 Retire the standalone `ConfirmIcon`: migrate its five call sites (`mcp_shares.templ:159`, `agent_mcp_endpoint.templ:152,334`, `mcp_nodes.templ:150`, `project_settings.templ:1278`) to `ui.ConfirmDialog`'s own icon circle, delete `components/confirm.templ`, and modify/remove the corresponding main-spec requirement in this change (the precondition is already met — `ui.ConfirmDialog` already renders that circle).
- [ ] 4.6 Add visible autosave feedback that is **additive to the existing toast contract**, not a replacement: the `inline-form-saving` capability already requires transient success/error toasts, and the active `web-ui-e2e-coverage` change pins `hx-post` + `hx-trigger="change delay:400ms"` + `hx-swap="none"` for settings autosave. So an in-flight indicator and a failure surface are required; a swapped in-field "saved" marker must be reconciled with `hx-swap="none"` and the toast requirement rather than asserted over it. Unit test asserts the indicator markup for in-flight and failed states.
- [ ] 4.7 Update the existing `project-settings-ui` "Edit the project info" scenarios and the `settings-navigation` "Keep section save behavior intact" scenarios so in-context failure is permitted alongside the page-level success message, removing the contradiction with 4.8.
- [ ] 4.8 Add in-context failure feedback for form submissions: a submission rejected for a non-field reason SHALL surface in the form's own context, not only as a page-level notice. Unit test asserts the contextual error markup.
- [ ] 4.9 Define and apply one table-versus-card-list rule: `ListRow`/`cardList` for icon+title+meta lists, `TableCard` for genuinely tabular data; convert `api_tokens.templ:126` accordingly.
- [ ] 4.10 Add mobile card fallbacks for the tables that lack them (backups, skills, schedules, members, api tokens, usage), following the existing pattern in `ui.templ:224-229,291-297` and `blueprints.templ:742,758`.
- [ ] 4.11 Derive `spotlightItems` (`spotlight.templ:24-30`) from `sidebarGroups()` (`ui.go:71`) so ⌘K covers every nav destination; unit test asserts palette coverage equals nav coverage (the "palette lists every destination" scenario is e2e-only).
- [ ] 4.12 Verification: `go test ./...`, `task lint`, `task e2e:test`, plus a manual browser pass at mobile and desktop widths over the changed pages.

## 5. Accessibility

- [ ] 5.1 Make `data-href` row targets keyboard-reachable (`ui.templ:305-309` agent cards, `schedules.templ:95` rows): add the role/tabindex treatment (or a real link/button) without changing visuals. Unit test asserts a focusable target per row; the visible-focus assertion is a source-level contract (a focus-visible class is present) verified by the browser audit, not by a Go unit test.
- [ ] 5.2 Emit a shared row-action style with a `@media (hover:none)` rule giving icon-only row actions `min-width:44px; min-height:44px`. Assert the rule's presence in the compiled CSS (source-level) — hit geometry is not assertable from HTML, so the behavioural check is the browser audit.
- [ ] 5.3 Associate a `<label for>` with the API-token name input (`api_tokens.templ:277-280`).
- [ ] 5.4 Add per-field validation error presentation for internal forms, so a failed submit identifies the field instead of only a top-of-page toast, with the control programmatically associated with its error.
- [ ] 5.5 Add explicit status work: inventory every status/state indicator that signals through colour or animation only (including `chat.templ`'s `memory-run-status` dot), and give each a text or shape signal. Unit test asserts the signals exist.
- [ ] 5.6 Verification: `task e2e:test` green, and the browser accessibility audit over the changed pages reports no new violations (this is where the touch-target and focus-visibility contracts are actually exercised).

## 6. Copy pass

- [ ] 6.1 Normalize list-empty headings to the canonical "No <thing> yet" form, replacing the mixed forms the audit listed. **Exempt** search-result/no-match states and "not set"/"none configured" states, which are not empty lists. Name the owners whose pinned strings must be updated in the same change: `external-mcp-nodes` ("no external nodes connected", "no tools"), `agent-dashboard-ui` ("no tools configured"), `project-settings-ui` ("no overrides"), active `add-api-tokens-ui` ("no tokens"), active `add-backups-ui` ("no backups").
- [ ] 6.2 Normalize load-failure headings to the canonical "Couldn't load <thing>" form, and reconcile the "unavailable" phrasings in `usage-dashboard`, `schema-editing`, and `blueprint-api` if any is a rendered gateway heading.
- [ ] 6.3 Remove internal implementation detail from user copy (`backups.templ:72`, `usage.templ:40`) and move over-long subtitles to body copy (`mcp_nodes.templ:30,57`, `mcp_shares.templ:26`).
- [ ] 6.4 Rewrite the awkward strings (`org_members_ui.templ:295`, `api_tokens.templ:457`, `share_page.templ:730,738`, `documents.templ:57`, `ui.templ:262`) and fix naming drift ("Add with assistant" vs "Assistant"; "API Tokens" vs "API tokens"; "MCP sharing" vs "MCP Sharing").
- [ ] 6.5 Unit test asserting the empty-state and error-heading vocabularies are restricted to the canonical forms, driven by an enumerated canonical-name map (the general "one name" rule is not testable without one).

## 7. Cross-repo dependencies (go-daisy — not implementable in this change)

Tracked here so they are not lost; each requires a PR in `emergent-company/go-daisy`, which has no
OpenSpec root. Do not start these as part of this change.

- [ ] 7.1 Back-port a secret-reveal surface from `components/secret.templ` (`SecretRevealModal` + `SecretRevealPanel`).
- [ ] 7.2 Back-port the meta rows from `components/meta.templ` (`MetaRow`/`MetaRowCustom`/`MetaGrid`/`EmptyDash`).
- [ ] 7.3 Extend `form.ToggleInput` with its label/tip row (upstream doc already names the gap).
- [ ] 7.4 Extend `form.Select` with `Groups []SelectOptionGroup` (native `<optgroup>`).
- [ ] 7.5 Extend `ui.CodeBlock` with a copy affordance (or add a snippet card).
- [ ] 7.6 Extend `ui.ListRow` with `Href` + `Chevron`, then replace the local `components.ListRow`.
- [ ] 7.7 (Removed — `ui.ConfirmDialog` already owns the icon circle; the local `ConfirmIcon` is retired in this change, see 4.5.)
- [ ] 7.8 After each upstream merge: re-pin `go-daisy` in `go.mod`, regenerate `vendor/`, and delete the corresponding local component here.
- [ ] 7.9 Repair the go-daisy stylesheet that this app `@import`s: close the unclosed braces at `components/css/custom.css:7` and `:17` (not `:13` — its braces balance overall, but `.sidebar-menu {` at `:7` and `.menu-item-link.menu-item {` at `:17` are never closed at the right nesting), remove the stray `*/` with no opener at `:51`, and remove the dead daisyUI-v4 variable declarations (`--bc` at `:54,67,84,105,264,277,284,295,317`; `--p`/`--s` at `:324`) that v5 never defines. This cannot be done here because the file is generated from go-daisy — task 1.4 deliberately does not patch the vendored copy.

- [ ] 7.10 Split `ui.Section`: extract `SectionHeading` + `Card` + `EmptyState`, and replace `Raw`/`Rows`/`Titleless`/`Wrapperless` with one chrome enum; remove `Margin`/`CardClass`/`BodyClass`.
- [ ] 7.11 `ui.Card(compact ...bool)` → a `Size` variant; drop the deprecated `CardCompact`.
- [ ] 7.12 `ui.Badge`: collapse `Dot`/`Animate`/`Icon`/`Glyph` (four mutually exclusive prefix modes) into one prefix variant; drop raw `Color` except for data-driven accents.
- [ ] 7.13 Remove the domain map from the generic library (`ui.StatusIntentFor` hard-codes active/open/completed/approved/pending/draft) and the colour string surgery in `IconTile`/`iconBgClass` in favour of the shared tone type.
- [ ] 7.14 `ui.EmptyState`: replace the overlapping presentation axes (`Variant`, `IconStyle`, `Bordered`, `Heading`) with a chrome enum plus a scale enum.

- [ ] 7.15 Replace go-daisy's literal radius with theme-driven utilities: 15 `rounded-lg`, 5 `rounded-sm`, 5 `rounded-xl`, 2 `rounded-md`, 4 `rounded-2xl`, 13 bare `rounded` — notably `ui/stat-card.templ:22`, `ui/empty-state.templ:206,208`, `nav/page-title.templ:13,47,99,107,143,179,203,219`, `ui/icon-tile.templ:38`, `table/table.templ:62`, `form/prompt-bar.templ:296,298,300,407,580`.
- [ ] 7.16 Fix the dead `rounded-btn` class (`form/palette.templ:125`, `form/combobox.templ:70`): no such class exists in daisyUI 5, so those elements currently get **zero** radius; use `rounded-field`.
- [ ] 7.17 Remove go-daisy's own padding overrides on daisyUI roots so density can be central: `ui/card.templ:35`, `ui/stat-card.templ:56`, `ui/dashboard.templ:37`, `table/table-card.templ:14,22,29`, `table/datagrid.templ:136`, `form/prompt-bar.templ:30,53,407`, `ui/tabs.templ:72,127,188`, the dropdown `p-1`/menu `p-2` sites, `modal/modal.templ:384`, `ui/command-palette-button.templ:30`, plus the fixed-pixel metrics in `components/css/custom.css:4,52-111,263-320`.
- [ ] 7.18 Stop redefining Tailwind's type scale from the library: `assets/app.css:57-65` overrides `--text-xs..xl`, which changes every consumer's typography.
- [ ] 7.19 Replace go-daisy's colour string concatenation (`ui/icon-tile.templ:38` `"bg-"+tone+"/10"`, `ui/stat-card.templ:88` `strings.Replace(p,"text-","bg-",1)`) with tokens, since concatenated class names bypass the theme pipeline.

## 8. Out-of-scope findings to file as GitHub issues

- [ ] 8.1 File the go-daisy back-port/extend/repair list (§7) as go-daisy issues (search for duplicates first, get confirmation before creating).
- [ ] 8.2 File the deferred go-daisy `table` primitive adoption note and the `web-ui-css` cross-surface width invariant gap (chat `100rem` vs runs `6xl` vs sidepanel modal) as tracked issues if not adopted in §2.3.

## 9. Conventions

Delivered by unit 1 (`web-ui-component-conventions`, #874 — merged): the `apps/web-ui/gateway/AGENTS.md`
section and the capability spec already live on `main`. This change does not re-add them.

- [x] 9.1 Add the component-conventions section to `apps/web-ui/gateway/AGENTS.md` — delivered in #874.
- [x] 9.2 Add the `web-ui-component-conventions` capability spec — delivered in #874.
- [x] 9.3 Add the mechanised convention checks — delivered in #874.
