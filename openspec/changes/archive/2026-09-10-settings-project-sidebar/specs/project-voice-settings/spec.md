## MODIFIED Requirements

### Requirement: Show the Voice settings section

The Settings area SHALL include a Voice sub-page, reachable from the settings sub-navigation, that displays the project's voice configuration and the master enable toggle.

#### Scenario: Voice section present

- **WHEN** the Voice settings page loads
- **THEN** a Voice section is shown with the master enable/disable toggle and the editable voice fields
