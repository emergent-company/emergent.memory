## 1. OpenSpec artifacts

- [x] 1.1 Author `proposal.md`, `tasks.md`, and the `search` delta spec `specs/search/spec.md` documenting the default weighted-fusion semantics and the relationship leg's treatment under each strategy (Refs #1002, #996). Verify: `openspec validate document-search-fusion-defaults --strict` passes.
