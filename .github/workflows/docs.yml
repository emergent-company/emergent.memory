name: Docs

on:
  push:
    branches:
      - main
    paths:
      - 'docs/**'
      - 'mkdocs.yml'
    tags:
      - 'v*'
  pull_request:
    paths:
      - 'docs/**'
      - 'mkdocs.yml'

jobs:
  build:
    name: Build docs
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # mike needs full history

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install -r docs/requirements.txt

      - name: Build docs (strict)
        run: mkdocs build --strict

  deploy-latest:
    name: Deploy latest (main)
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install -r docs/requirements.txt

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Deploy as latest
        run: mike deploy --push --update-aliases dev latest

      - name: Sync root-level files to gh-pages
        run: |
          # mike deploys versioned docs but root-level raw files need to be at /
          # (swagger.json, swagger.yaml, openapi.json, llms*.md)
          git fetch origin gh-pages
          git checkout gh-pages
          cp docs/swagger/swagger.json swagger.json
          cp docs/swagger/swagger.yaml swagger.yaml
          cp docs/swagger/swagger.json openapi.json
          cp docs/llms.md llms.md
          cp docs/llms-go-sdk.md llms-go-sdk.md
          cp docs/llms-swift-sdk.md llms-swift-sdk.md
          git add swagger.json swagger.yaml openapi.json llms.md llms-go-sdk.md llms-swift-sdk.md
          git diff --cached --quiet || git commit -m "docs: sync root-level raw files [skip ci]"
          git push origin gh-pages

  deploy-version:
    name: Deploy versioned release
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install -r docs/requirements.txt

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Extract version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/}" >> "$GITHUB_OUTPUT"

      - name: Deploy versioned docs and update latest alias
        run: |
          mike deploy --push --update-aliases "${{ steps.version.outputs.VERSION }}" latest
          mike set-default --push latest

      - name: Sync root-level files to gh-pages
        run: |
          git fetch origin gh-pages
          git checkout gh-pages
          cp docs/swagger/swagger.json swagger.json
          cp docs/swagger/swagger.yaml swagger.yaml
          cp docs/swagger/swagger.json openapi.json
          cp docs/llms.md llms.md
          cp docs/llms-go-sdk.md llms-go-sdk.md
          cp docs/llms-swift-sdk.md llms-swift-sdk.md
          git add swagger.json swagger.yaml openapi.json llms.md llms-go-sdk.md llms-swift-sdk.md
          git diff --cached --quiet || git commit -m "docs: sync root-level raw files [skip ci]"
          git push origin gh-pages
