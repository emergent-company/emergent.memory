# Gateway Go Guidelines

Modern, idiomatic Go — always. Source of truth: the `use-modern-go` skill (JetBrains/go-modern-guidelines).

## Version
- `go 1.26` (from `go.mod`). Use only idioms available up to 1.26.

## How to write Go here
- Before editing any `.go` file, run the guideline CLI and follow what applies:

  ```sh
  sh ../.opencode/skills/use-modern-go/scripts/run-tool.sh list --file-path <file>.go
  ```

- Read the full list (newest first). Treat returned guidelines as authoritative, even over nearby older code.
- Skip a guideline only if it won't compile, changes behavior, or clearly doesn't match the edited code.

## Core idioms
- `for i := range n` — never `for i := 0; i < n; i++`.
- Built-in `min`/`max` over handwritten comparisons.
- `slices.*`, `maps.*`, `cmp.Or` over hand-rolled loops.
- `errors.Is`/`errors.As`/`errors.Join` over `==`/manual wrapping.
- `strings.Cut`/`CutPrefix`/`CutSuffix` over `Index`+slice or `HasPrefix`+`TrimPrefix`.
- `wg.Go` over `wg.Add(1)` + `go func(){ defer wg.Done() }()`.
- `t.Context()` in tests; `b.Loop()` in benchmarks.
- `any` over `interface{}`.

## Generated files
- `*_templ.go` is generated by templ. Never hand-edit; run `templ generate`.

## Verify (every change)
Run from this directory (`apps/web-ui/gateway`):
```sh
go build ./...
go test ./...
task lint
```
