---
description: Analyze a Memory agent session by ID and suggest improvements.
agent: build
---

Analyze the Memory agent session with ID `$ARGUMENTS` using the `session-analyze` skill.

Fetch the session dump, determine goal/outcome/frictions, and produce concrete improvement suggestions in the skill's report format. Report findings and recommended changes; propose before modifying any config or code.
